# M5 可扩展与 v0.3（5 周）

| 编号 | 任务 | 仓库 | 模式 | 规格 | 关键验收 |
|---|---|---|---|---|---|
| M5-01 | 多实例网关与指令路由（Valkey Stream） | panel | 团队：panel-backend、e2e-tester + concurrency-reviewer | 40 | 3 个网关实例下处置时延 P99 < 1 s |
| M5-02 | HTTPS 长轮询备用通道 | panel、node-agent | 团队：panel-backend、agent-dev | 20 | 阻断 WebSocket 后自动切换，不丢不重 |
| M5-03 | 机器模式 | panel、node-agent | 团队 | 21 | 一个进程承载多个节点 |
| M5-04 | Agent 签名自升级与灰度 | panel、node-agent | 单会话 + security-reviewer | 40 | 验签失败拒绝；新版连不上自动回退 |
| M5-05 | 邀请返利 | panel | 单会话 + billing-verifier | 13 | 冻结期、退款作废、自邀请不计 |
| M5-06 | 备份与恢复 | panel | 单会话 | 40 | CI 定期恢复演练 |
| M5-07 | Helm Chart | panel | 单会话 | 40 | kind 集群端到端通过 |
| M5-08 | 基准与混沌测试 | panel、node-agent | 工作流 | 42 | spec/42 指标可复现 |
| M5-09 | 吸收上游：Xboard-Node 与内核 fork | node-agent、sing-box、xray-core | 工作流（core-sync） | 21 | 挑选合入的修复通过一致性测试 |
| M5-10 | 发布 v0.3 | 全部 | 工作流（release-audit） | 42 | 无严重问题 |

## v1.0 前
- 冻结 `proto/node/v1`、`/v1` 客户端接口与管理接口，公布兼容与安全支持政策。
- 对照实现复核一次规格。
- 从 V2Board / Xboard 的迁移工具（spec/13）。

## 1.0 之后（随自研客户端）
- `diagnostics` 模块（`POST /v1/diagnostics`，spec/30）：诊断日志由自研客户端提交，客户端在 1.0 之后（workspace CLAUDE.md 仓库表）。先补 spec/13 小节（保存期限、查看权限、个人信息处理，CONV-24、CONV-29），再实现；实现前不可开启（OPS-08）。
- 客户端安装包发布签名（`GET /v1/releases/latest` 的 `Release.signature`、`key_id`，CONV-30）：先比照 spec/40 DEP-09 定义签名输入（带域分隔前缀，绑定平台、版本与安装包 SHA-256）与测试向量，写入规格与 panel-spec，再实现发布流程签名、控制面下发与客户端验签。契约目前只约束 `key_id` 格式（panel-spec v0.6.1）。
