# M0 脚手架与验证（2 周）

目标：契约定稿到可开发程度；控制面、两个前端、模拟 Agent 都有可构建可测试的骨架；Xboard-Node 审计完成。

## M0-01 规格评审 [已完成 workspace#1]
- 仓库：workspace
- 模式：工作流（`workflows/spec-review.md`）
- 依赖：无
- 交付：`review/spec-review-<日期>.md`；负责人确认后的规格修改
- 验收：
  1. 每条发现经过对抗复核，报告标明保留或驳回理由。
  2. 所有“严重”级发现已在 `spec/` 中修正或写入 ADR。

## M0-02 建立仓库与 CI 骨架 [已完成 workspace#6、#7，node-agent#2，sing-box#1，xray-core#1，client#1]
- 仓库：全部
- 模式：单会话
- 依赖：无
- 交付：运行 `scripts/bootstrap.sh --org <组织>` 后，在 GitHub 创建 `workspace`、`panel-spec`、`panel`、`node-agent`、`sing-box`、`xray-core`、`client`、`.github` 并推送；各仓 CI 骨架；`.github` 仓库的 SECURITY.md、CODE_OF_CONDUCT.md、PR 模板
- 验收：
  1. 每个仓库的 CI 通过，包含 SPDX 头检查与依赖许可证扫描（node-agent 中原有 MPL-2.0 文件按例外登记）。（各仓 CI 含 SPDX/REUSE 检查与许可证扫描。）
  2. `node-agent/go.mod` 的 `replace` 改为指向本组织的 `sing-box`、`xray-core`（`panel-base` 分支对应提交），`make build` 成功。（`replace` 指向 akari-project fork 的 `panel-base`，与原锁定提交代码无差异；`make build` 通过。）
  3. 另一台机器上克隆 `workspace` 并运行 `scripts/clone-all.sh <组织>` 后得到相同布局。（`clone-all.sh` 在全新目录得到与本地一致的布局，workspace#6。）
- 后续：panel 仓库补 DCO 检查（spec/42 42.6，panel#5）；在 GitHub 仓库设置中开启 Report content（需组织管理员手动操作）。

## M0-03 panel-spec：节点协议、客户端接口、管理接口 [已完成 panel-spec#1]
- 仓库：panel-spec
- 模式：单会话 + protocol-reviewer
- 依赖：M0-01
- 规格：spec/20、spec/30、spec/31、spec/02
- 交付：`proto/node/v1`（基于起步包草案）；`openapi/client/v1.yaml`（基于草案补齐 spec/30 全部接口）；`openapi/console/v1.yaml`（spec/31 全部资源，带 `x-permission` 与 `examples`）；`openapi/client/v1.yaml` 同时覆盖 spec/32 各页面所需字段（spec/30 API-10）；接入接口 `enrollment.proto`（spec/20 NODE-18）；入站 settings 的 JSON Schema（spec/21 AGT-13）；生成的 Go 模块与 TypeScript 类型；Makefile
- 验收：
  1. `buf lint`、`buf build`、两份 OpenAPI 的 Redocly lint 通过。
  2. 每个 OpenAPI 操作至少有一个响应示例，供 Mock 服务使用。
  3. 打 tag `v0.1.0`。（后续修订见 panel-spec v0.2.0，panel-spec#3。）

## M0-04 Xboard-Node 代码审计与改造方案 [已完成 node-agent#1]
- 仓库：node-agent
- 模式：单会话 + 子代理（并行阅读通信层、内核集成、计量三部分）
- 依赖：M0-02
- 规格：spec/21
- 交付：`node-agent/FORK_PLAN.md`：包结构地图；通信层边界（要替换的包与接口）；内核抽象与 spec/21 §21.3 接口的对应关系；三项能力的现状；流量上报是否可靠（断线重传、去重）；`UPSTREAM.md` 初版
- 验收：
  1. 三项能力（增删用户不影响他人、计量准确、移除后关闭会话）在两个内核上各有结论与证据（测试或代码位置）。
  2. 列出需要修改协议草案的地方；若有，已在 M0-03 中合并。（FORK_PLAN 第 6 节列出 4 项；M0-03 的 v0.1.0 已发布，协议修改改由 panel-spec v0.2.0 完成（含破坏性变更，故为小版本）：mKCP `finalmask`、`Credential` 各协议形式与施加于全部入站，规格部分见 spec/21 AGT-12、AGT-14、AGT-15。）
  3. 估算 M3 各任务工作量。

## M0-05 控制面骨架 [已完成 panel#2]
- 仓库：panel（server）
- 模式：单会话
- 依赖：M0-03
- 规格：spec/40、spec/41、spec/02、spec/03
- 交付：`cmd/panel`（api / gateway / worker / all）；配置加载与校验；slog；`/healthz`；goose 迁移（起步包 `00001_init.sql`）；sqlc；testcontainers 基座；可注入时钟；`internal/webui`（嵌入前端、按 Host 路由、SPA 回退、缓存与安全头、运行时配置注入，spec/40 DEP-01 至 DEP-06）；`panel migrate` 与 `panel admin create` 子命令；00001 按 spec/03 修订后提交；Makefile
- 验收：
  1. `make test` 在全新机器上通过。（`make test` 带 `-race`，集成测试经 testcontainers 在 PostgreSQL 18 上运行；CI 设 `PANEL_REQUIRE_DB=1`。）
  2. 迁移在 PostgreSQL 18 上执行成功；只追加表、价格行、内核协议校验的触发器各有测试。（`internal/db/schema_test.go` 覆盖只追加表、价格行、内核协议触发器；`cmd/panel` 的 `TestBinary` 在空库上执行 `panel migrate`。）
  3. 三个角色可分别启动，也可同进程启动。（`internal/app` 的 `TestRolesStartSeparatelyAndTogether`；`TestBinary` 以真实进程启动 api / gateway / worker / all，检查 `/healthz` 与 SIGTERM 优雅退出。）
  4. `make build` 产出单一二进制，内含两个前端；`-tags noui` 构建不含前端；`index.html` 中的运行时配置由服务端注入。（`make build` 嵌入两个前端，`panel version` 报告 `ui true`；`make build-noui`；`check-embed` 拒绝提交不一致的前端产物；`webui_test.go` 覆盖运行时配置注入。）

## M0-06 前端骨架与 Mock 服务 [已完成 panel#1]
- 仓库：panel（web）
- 模式：单会话（frontend）
- 依赖：M0-03
- 规格：spec/41、spec/32
- 交付：pnpm workspace（`portal`、`admin`、`ui`、`sdk`）；SDK 生成；`pnpm mock` 用 Prism 按两份 OpenAPI 启动 Mock；主题（深浅色）；i18n；路由与布局框架；CI
- 验收：
  1. `pnpm -r lint typecheck test build` 通过。
  2. 两个应用在 Mock 服务下可登录（Mock 返回的令牌）并进入空白首页。
  3. OpenAPI 变更后 `pnpm gen` 更新 SDK，类型错误在 CI 中暴露。
  4. 构建产物只使用相对路径，运行时配置从 `window.__PANEL_CONFIG__` 读取，可被 M0-05 嵌入。

## M0-07 模拟 Agent [已完成 panel#4]
- 仓库：panel（server/e2e/fakeagent）
- 模式：单会话 + concurrency-reviewer
- 依赖：M0-03
- 规格：spec/20
- 交付：完整实现握手、会话加密、信封、seq/ack 重传、状态与流量上报的模拟 Agent；可编程注入断线、延迟、重复、丢包；可配置上报的内核能力
- 验收：
  1. 协议一致性测试套件（`e2e/conformance`）以模拟 Agent 为对象全部通过；该套件设计为之后可直接对真实 Agent 运行。（套件覆盖 spec/20 20.6，panel-spec 测试向量全部通过；`CONFORMANCE_AGENT=exec` 可对真实 Agent 运行。）
  2. 可同时模拟 1,000 个节点。（`TestScale_1000Nodes`：`-race` 下 1,000 节点连接收敛 1.49 s、重连 1.37 s，每节点 6 个协程约 85 KiB。）

## M0-08 本地环境与 Claude Code 配置验证 [待办]
- 仓库：workspace
- 模式：单会话
- 依赖：M0-02
- 验收：
  1. 修改生成代码与已提交迁移被 hook 拒绝；编辑 Go 文件后自动格式化。
  2. 在 workspace 中以 Agent Teams 组建两名队员完成一个示例任务。
  3. README 记录每位开发者需要安装的工具与版本。
