# ADR 0004：节点通信：WSS + Protobuf + 会话加密

- 状态：已接受
- 日期：2026-09

## 背景
需要秒级下发、可经 CDN、CDN 不应看到用户凭据、断线不丢数据。

## 决定
WSS 为主通道、HTTPS 长轮询为备用；握手用每节点 PSK 的 HMAC 加 X25519 临时密钥协商会话密钥，信封 XChaCha20-Poly1305 加密；至少一次投递 + 幂等键。节点通过一次性接入令牌换取长期凭据。

## 影响
实现复杂度高于简单的 HTTP 轮询；需要完整的端到端与混沌测试覆盖。
