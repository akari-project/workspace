# ADR 0011：插件：编译期与进程外两种形式

- 状态：已取代（被 ADR 0015）
- 日期：2026-09

## 背景
AGPL 下编译进控制面的插件须随服务公开源码；部分运营者需要闭源支付渠道。

## 决定
支付、通知、验证码插件同时支持编译期注册与进程外（带签名的 HTTP 回调协议，定义在 panel-spec/openapi/plugin）。

## 影响
进程外插件多一次网络往返；协议需要版本管理。
