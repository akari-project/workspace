<!-- SPDX-License-Identifier: Apache-2.0 -->

# M1-01 开工前的规格缺口评审（2026-09-24）

形式：lead 提出 5 项建议，`spec-owner` 代理作为反方对抗评审两轮；lead 逐条核实反方引用的证据（console v1.yaml 13085、client v1.yaml 3017、两份 OpenAPI 的 `info.version`、00001 766 与 794 行），均属实。负责人事先同意按 lead 的建议处理，并要求先经过辩论。

## 结论

| # | 建议 | 反方意见 | 定案 | 落实 |
|---|---|---|---|---|
| 1 | 管理员注册码推迟到 M1-07 | 同意，但推迟须写入 backlog；`redeem_codes` 不能复用 | 采纳；`invite_code` 查找抽象为接口 | backlog M1-01、M1-07 |
| 2 | 新增注册策略设置键 | 反对新起名：console OpenAPI 已有 `registration_policy`、`email_domain_allowlist`、`email_domain_denylist` | 沿用已有字段名，spec/03 登记键名；补“两个名单同时设置”的语义 | spec/03 3.6、spec/10 AUTH-02 |
| 3 | 在 Valkey 缓存新令牌对 | 违反 CONV-20，须写 ADR；用时钟与 `used_at` 判定；写明缓存未命中时的行为 | 采纳。窗口内且指纹一致但未命中时返回 `invalid_grant`，不签发、不吊销；窗口外或指纹不符时吊销整条链；放弃 HMAC 推导 | ADR 0017、spec/02 CONV-20、spec/10 AUTH-07 |
| 4 | `sessions.family_expires_at` | 改名为 `absolute_expires_at`，同时承载 AUTH-21；可空以兼容旧二进制（DEP-12） | 采纳 | spec/03 3.6、spec/10 AUTH-07 |
| 5 | M1-01 先做最小 SMTP 投递 | 须走 `Notifier` 与 `notification_outbox`；SMTP 密码存放没有定义 | 采纳。SMTP 密码存为 settings 键 `smtp_password_enc`；CONV-19、CONV-30、AUTH-18 一并覆盖 settings 键；M1-01 只实现无密码 SMTP | spec/02 CONV-19、CONV-30，spec/10 AUTH-18，backlog M1-01 |

## 反方提出的其他问题

| 问题 | 处理 |
|---|---|
| client OpenAPI 说刷新令牌 Cookie “只随 `POST /v1/oauth/token` 发送”，与 AUTH-08 的 `__Host-` 加 Path=/ 矛盾 | panel-spec 修改描述 |
| 注册时生成共用代理凭据（AUTH-13）、登录时注册设备（AUTH-10），M1-01 的交付清单里没有写 | 写入 backlog M1-01 范围说明 |
| OpenAPI 的 `info.version` 是 0.1.0，发布 tag 是 v0.2.1 | panel-spec 修正 |
| `/v1/config` 没有注册策略，门户无法决定是否显示注册入口与邀请码输入框 | 增加可选字段 `registration_policy`（CONV-14），缺省按 `open` 处理，以服务端校验为准；不下发域名名单；修改后重新生成签名与 ETag（spec/30） |
